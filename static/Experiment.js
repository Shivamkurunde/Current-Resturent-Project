function display(sectionId) {
    // For Flask implementation, we'll handle navigation via routes
    window.location.href = '/' + sectionId;
}

// Initialize - show only the home section
window.onload = function() {
    // This will be handled by Flask routing
};